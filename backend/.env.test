# Test Environment Configuration
NODE_ENV=test
PORT=4001
APP_URL=http://localhost:3000

# Test Database Configuration (in-memory)
DB_PATH=:memory:

# Test Email Configuration (mock)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=test@example.com
SMTP_FROM_NAME=Ads.txt Manager Test

# Test Security
TOKEN_SECRET=test_secret_key