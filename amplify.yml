amplify:
  - name: adstxt-manager
    backend:
      type: sandbox
      config:
        config: "{\"type\":\"global\"}"
    frontend:
      config:
        sourceDir: frontend
        framework: react
        BuildCommand: npm run build
        StartCommand: npm run start